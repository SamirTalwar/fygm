#!/bin/bash

set -eu

branches=($( (git branch --merged | grep -E -v "^\*| $(git main-branch)$| master$" | sed 's/^ *//') || : ))
if [[ -n "${branches[*]+x}" ]]; then
  git branch -d "${branches[@]}"
fi
