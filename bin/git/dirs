#!/bin/bash

set -e

find . -maxdepth 1 -type d | while read dir; do
    if [[ -e $dir/.git ]]; then
        echo \"In $(basename $dir): git $@\"
        (cd $dir && git $@)
        echo
    fi
done
true
