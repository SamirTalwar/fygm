#!/usr/bin/env zsh

set -e

file="/tmp/watching-it-$(tmux display-message -p '#{session_id}-#{window_index}')"

if [[ -e $file ]]; then
    cat $file >/dev/null
    rm -f $file
fi
mkfifo $file

while true; do
    zsh -c "clear; $(cat $file)" || :
done
