#!/usr/bin/env zsh

set -e
set -u

for arg in $@; do
  if [[ $arg != 'beep' ]]; then
    echo >&2 "I don't know how to \"${arg}\"."
    exit 1
  fi
done

for arg in {0..$#}; do
  print -n '\a'
  sleep 0.5
done
